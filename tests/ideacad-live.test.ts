import{describe,expect,it,vi}from'vitest';import{createFrameCoalescer,createMemoryIdeacadLive,frameAllowed,rosterIsLive}from'../src/lib/ideacad/live';describe('IdeaCAD live',()=>{it('filters foreign and backward frames',()=>{const roster=new Set(['mine']);expect(frameAllowed({documentId:'peer',conceptId:'c',revision:2,features:{}},roster,1)).toBe(false);expect(frameAllowed({documentId:'mine',conceptId:'c',revision:0,features:{}},roster,1)).toBe(false)});it('delivers memory frames',()=>{const live=createMemoryIdeacadLive(),fn=vi.fn();live.subscribeFrames('d',fn);live.sendFrame({documentId:'d',conceptId:'c',revision:1,features:{}});expect(fn).toHaveBeenCalledOnce()});it('coalesces preview frames',()=>{vi.useFakeTimers();const send=vi.fn(),q=createFrameCoalescer(send,4);q.queue({documentId:'d',conceptId:'c',revision:1,features:1});q.queue({documentId:'d',conceptId:'c',revision:2,features:2});vi.advanceTimersByTime(250);expect(send).toHaveBeenCalledTimes(1);expect(send.mock.calls[0][0].revision).toBe(2);q.stop();vi.useRealTimers()});it('derives twenty-second live recency',()=>expect(rosterIsLive(1000,21000)).toBe(true))});
