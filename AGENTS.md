# AGENTS.md

Read `CLAUDE.md` in full before doing anything. It is the operating rules for every
agent working in this repository, whichever vendor runs it; this file exists only so a
Codex task finds it, and it carries no rule of its own. Then read
`docs/CODEX_ENVIRONMENT.md` for what this container was set up with and what it was
not.
