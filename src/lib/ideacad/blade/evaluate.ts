import type { BladeConfig } from './materials'; import { featureOf, type BladeTree } from './tree';
const IN_CM=2.54;
export interface RuleResult { id:string; label:string; value:number; limit:string; pass:boolean }
export interface Evaluation { diameterIn:number; fullHeightIn:number; hexExtensionIn:number; massG:number; comHeightIn:number; inertiaGcm2:number; radiusOfGyrationCm:number; rules:RuleResult[]; unverifiedStandardParts:boolean; geometry:{stations:{r:number;z:number}[]; bladePolygon:{x:number;y:number}[]; bladeCount:number; bladeZ:number; hexAcrossFlats:number; hexHeight:number} }
export function frustumProperties(r1:number,r2:number,h:number,density:number){ const volume=Math.PI*h*(r1*r1+r1*r2+r2*r2)/3;const mass=volume*density;const inertia=Math.abs(r2-r1)<1e-9?.5*mass*r1*r1:(3/10)*mass*(r2**5-r1**5)/(r2**3-r1**3);const centroid=h*(r1*r1+2*r1*r2+3*r2*r2)/(4*(r1*r1+r1*r2+r2*r2));return{volume,mass,inertia,centroid}; }
export function polygonProperties(points:{x:number;y:number}[]){let area2=0,ix=0,iy=0,cx6=0,cy6=0;for(let i=0;i<points.length;i++){const a=points[i],b=points[(i+1)%points.length],c=a.x*b.y-b.x*a.y;area2+=c;cx6+=(a.x+b.x)*c;cy6+=(a.y+b.y)*c;ix+=(a.y*a.y+a.y*b.y+b.y*b.y)*c;iy+=(a.x*a.x+a.x*b.x+b.x*b.x)*c;}const sign=Math.sign(area2)||1, area=Math.abs(area2)/2;return{area,cx:cx6/(3*area2),cy:cy6/(3*area2),polar:sign*(ix+iy)/12};}
export function bladePlanform(rootWidth:number,tipWidth:number,length:number,sweepDeg:number,mountRadius:number){const shift=Math.tan(sweepDeg*Math.PI/180)*length;return[{x:mountRadius,y:-rootWidth/2},{x:mountRadius,y:rootWidth/2},{x:mountRadius+length,y:shift+tipWidth/2},{x:mountRadius+length,y:shift-tipWidth/2}];}
export function evaluate(tree:BladeTree,config:BladeConfig):Evaluation{const body=featureOf(tree,'revolve'),hex=featureOf(tree,'hexBoss'),sketch=featureOf(tree,'bladeSketch'),pattern=featureOf(tree,'circularPattern'),mount=featureOf(tree,'mount');const mat=config.materials.find(x=>x.id===tree.materials.body)!;const stock=config.stock.find(x=>x.id===tree.materials.bladeStock)!;let mass=0,I=0,mz=0,maxR=0;for(let i=1;i<body.stations.length;i++){const a=body.stations[i-1],b=body.stations[i],q=frustumProperties(a.r*IN_CM,b.r*IN_CM,(b.z-a.z)*IN_CM,mat.densityGcm3*tree.materials.bodySolidFraction);mass+=q.mass;I+=q.inertia;mz+=q.mass*(a.z*IN_CM+q.centroid);maxR=Math.max(maxR,a.r,b.r)}const side=hex.acrossFlats*IN_CM/Math.sqrt(3),area=3*Math.sqrt(3)*side*side/2,hmass=area*hex.height*IN_CM*mat.densityGcm3*tree.materials.bodySolidFraction;const bodyTop=body.stations.at(-1)!.z;mass+=hmass;I+=(5/12)*hmass*side*side;mz+=hmass*(bodyTop+hex.height/2)*IN_CM;maxR=Math.max(maxR,hex.acrossFlats/Math.sqrt(3));const poly=bladePlanform(sketch.rootWidth,sketch.tipWidth,sketch.length,sketch.sweepDeg,sketch.mountRadius);const pp=polygonProperties(poly.map(p=>({x:p.x*IN_CM,y:p.y*IN_CM})));const bmass=stock.densityGcm3*stock.thicknessIn*IN_CM*pp.area;mass+=bmass*pattern.count;I+=stock.densityGcm3*stock.thicknessIn*IN_CM*pp.polar*pattern.count;mz+=bmass*pattern.count*(mount.z+stock.thicknessIn/2)*IN_CM;maxR=Math.max(maxR,...poly.map(p=>Math.hypot(p.x,p.y)));for(const part of config.standardParts){mass+=part.massG;mz+=part.massG*bodyTop*IN_CM/2;}const com=mass?mz/mass:0;const diameterIn=maxR*2,fullHeightIn=bodyTop,hexExtensionIn=hex.height;const forward=tree.rotation==='cw'?sketch.sweepDeg>0:sketch.sweepDeg<0;const rules:RuleResult[]=[{id:'diameter',label:'Diameter',value:diameterIn,limit:`≤ ${config.rules.maxDiameterIn} in`,pass:diameterIn<=config.rules.maxDiameterIn},{id:'height',label:'Full height',value:fullHeightIn,limit:`${config.rules.minHeightIn}–${config.rules.maxHeightIn} in`,pass:fullHeightIn>=config.rules.minHeightIn&&fullHeightIn<=config.rules.maxHeightIn},{id:'hex-extension',label:'Hex extension',value:hexExtensionIn,limit:`${config.rules.minHexExtensionIn}–${config.rules.maxHexExtensionIn} in`,pass:hexExtensionIn>=config.rules.minHexExtensionIn&&hexExtensionIn<=config.rules.maxHexExtensionIn},{id:'mass',label:'Mass',value:mass,limit:`≤ ${config.rules.maxMassG} g`,pass:mass<=config.rules.maxMassG},{id:'engagement',label:'Engagement',value:sketch.sweepDeg,limit:'Inspector verifies visually',pass:forward}];return{diameterIn,fullHeightIn,hexExtensionIn,massG:mass,comHeightIn:com/IN_CM,inertiaGcm2:I,radiusOfGyrationCm:mass?Math.sqrt(I/mass):0,rules,unverifiedStandardParts:config.standardParts.some(x=>!x.verified)||config.launcher.acrossFlatsIn===null,geometry:{stations:body.stations,bladePolygon:poly,bladeCount:pattern.count,bladeZ:mount.z,hexAcrossFlats:hex.acrossFlats,hexHeight:hex.height}}}
