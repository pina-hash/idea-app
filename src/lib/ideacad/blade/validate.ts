import type { BladeConfig } from './materials'; import type { BladeTree } from './tree';
export interface BladeProblem { featureId:string; parameter:string; message:string }
export function validateBladeTree(tree: BladeTree, config?: BladeConfig): BladeProblem[] { const p:BladeProblem[]=[]; if(tree?.schema!==1||tree.editor!=='blade'||tree.units!=='in') return [{featureId:'document',parameter:'schema',message:'This is not a supported blade document.'}]; const body=tree.features.find(f=>f.type==='revolve'); if(!body||body.type!=='revolve') p.push({featureId:'body-revolve',parameter:'stations',message:'Add the body feature.'}); else { if(body.stations.length<3||body.stations.length>8)p.push({featureId:body.id,parameter:'stations',message:'Use 3 to 8 body stations.'}); body.stations.forEach((s,i)=>{if(s.r<0)p.push({featureId:body.id,parameter:`stations.${i}.r`,message:'Radius cannot be negative.'});if(i&&s.z<=body.stations[i-1].z)p.push({featureId:body.id,parameter:`stations.${i}.z`,message:'Station heights must increase.'})}); if(config&&body.stations[0]?.z!==config.tipHeightIn)p.push({featureId:body.id,parameter:'stations.0.z',message:'Start the body at the configured tip height.'}); } const pat=tree.features.find(f=>f.type==='circularPattern');if(pat&&pat.type==='circularPattern'&&(pat.count<2||pat.count>8||!Number.isInteger(pat.count)))p.push({featureId:pat.id,parameter:'count',message:'Use 2 to 8 blades.'}); if(tree.materials.bodySolidFraction<.1||tree.materials.bodySolidFraction>1)p.push({featureId:'materials',parameter:'bodySolidFraction',message:'Body fill must be between 10% and 100%.'}); return p; }
