# 0258 Unblock main's CI

- Issued: 2026-09-14
- By: Mr. Pina, mid-session, in response to ledger 0248's standing-down comment
  on PR #114.
- Owns: `src/lib/ideacad/viewport/picking.ts`, the failing assertions in
  `tests/dom/ideacad-ui-mount.test.ts`, `docs/prompt-ledger/entries/0258-*`,
  and its own `docs/history/` entry. **Nothing else.**
- Migration permitted: no. **Claims: none.**
- Status: pushed
- Branch: `claude/ideacad-unblock-0258`, branched from `main` at `eabed627`.
- Notes: `main` has been red on BOTH gates for every run since #104, which
  blocks every lane in the repository -- a worse cost than a PR that reaches
  outside one bundle's ownership, which is why this one is authorised to.
  **The `0215` applied record is deliberately NOT written**: the migration is
  genuinely unapplied, not missing paperwork, and `tools/record-applied.mjs`
  writes from Mr. Pina's own verification output, never from a session's
  belief. That check stays red until he pastes the migration and the record is
  written from what production answers. The verification query is in the PR
  body for him.
