# 0010 Regenerate the browser-verify counts block, stale from the 0005 and 0006 merge
- Issued: 2026-09-02
- By: router chat for IDEA portal work
- Owns: the generated counts block in `tools/browser-verify/README.md`, `docs/prompt-ledger/entries/0010-*`, and its own `docs/history/` entry.
- Migration permitted: no. Highest on origin/main at issue: 0170
- Status: deployed
- Branch: `claude/browser-verify-counts-regen-x8kn9h`. BRANCHED FROM `origin/integration` (`fe07200`), not `main`.
- Notes: CI run #510 on PR #71 (integration into main) failed
  `tests/derived-numbers.test.ts` on two assertions: the tree derives 66
  specs, 37 routes and 132 runs; the committed block says 65, 36 and 130.

  NEITHER BRANCH WAS WRONG ALONE. Prompt 0005 generated the block at
  `859c8d5` from a tree carrying 65 specs. Prompt 0006 added
  `tools/browser-verify/routes/feedback.mjs` on its own branch, where no
  block existed to regenerate. The merge at `fe07200` produced a tree whose
  static counts no longer match its own block. This is the integration-only
  failure `integration` exists to expose, and the test 0005 wrote for it
  caught it on its first real merge, which is the system working.

  The block is regenerated by its one writer, `npm run verify:readme`, which
  needs a browser and about six minutes. It is never edited by hand: a
  hand-written file never holds a computed value, and the block carries the
  commit and instant it was measured at.

  Deliberately excluded: any migration, any edit to `readme-counts.mjs`,
  `tests/derived-numbers.test.ts` or any route spec, and any merge to `main`.
