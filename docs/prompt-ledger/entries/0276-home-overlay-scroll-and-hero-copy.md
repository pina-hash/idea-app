# 0276 The home page will not scroll, and the hero subtitle
- Issued: 2026-09-22T00:00:00Z
- By: Report from James Morales, 2026-09-16, build `8b115a7`, role student, viewport 2707x1074, Chrome on Windows -- "I cant scroll on the home page", tried first: "I reloaded and tried different mouses". Plus report 13 (Mr. Pina, 2026-09-11) on the hero subtitle. Routed as ledger 0276.
- Owns: `src/lib/PathwayPicker.svelte`, `src/lib/tour/SpotlightTour.svelte`, `src/lib/tour/HomeTour.svelte`, `src/routes/+page.svelte`, `src/app.css` (the `.legacy-index` block and the `body` rule only), `tools/browser-verify/routes/` (one new or widened spec) plus the counts block in `tools/browser-verify/README.md`, `docs/prompt-ledger/entries/0276-*`, `docs/history/<branch slug>`.
- Migration permitted: no. Claims: none. Highest on origin/main at issue: 0217
- Status: pushed
- Branch: `claude/new-session-5uia88`
- Notes: Forbidden: `src/lib/AppLauncher.svelte`, `src/lib/portal-apps.ts`, `src/lib/profile.ts`, `src/lib/avatars.ts`, `src/lib/shell/**`. THREE OF THE PROMPT'S EIGHT CLAIMS WERE WRONG AGAINST THE TREE and the report says which: claim 2's MECHANISM (a fixed overlay does not absorb the wheel -- measured, the document moved its full range with the sheet up), claim 6 in full (the sticky header works; `body`'s computed `overflow-y` IS `auto` as claimed, but the header measured 0px movement against a 300px control at 375/1440/2707 on three routes, and the prescribed `overflow-x: visible` changed the reading not at all), and the existence of `Writing_Voice_Guide.md`, which is in no tree here -- `docs/standards/IDEA_CLAUDE_DESIGN_STANDARDS.md` "Content voice" was used instead. Claim 2's CULPRIT was right and is what shipped the fix: the sheet made a working scroll invisible (7.9% of viewport pixels changed across the entire 415px range against 27.5% with the overlay hidden, animations stilled, noise floor 0%). No sticky-header change was made, deliberately: the measurement says there is nothing to fix. The counts block is GENERATED (`npm run verify:readme -- --route`), not hand-edited, because a hand-edited digit reddens `tests/derived-numbers.test.ts`. Also touched, outside the Owns line and needed by it: `tools/browser-verify/measured/tour-mode-picker.json`, which is the generator's own per-spec output for the new spec.
