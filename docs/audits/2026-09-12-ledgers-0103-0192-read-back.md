# Read-back audit: ledgers 0103 through 0192

- 2026-09-12. Ledger 0193, `claude/gallant-mendel-xsgp0w`, from `origin/integration` at `7f5ca8f0`
- Window: 75 `docs/history/` entries, 2026-09-09 to 2026-09-12; migrations `0193`-`0208`; 89 new tests
- Changes nothing. Every item was re-checked against the tree before listing.

**Tree green as found**: `npm test` **433 files, 8318 tests, 0 failures**; `svelte-check`
**0 errors, 37 warnings in 20 files (31/5/1)**, so the `CLAUDE.md` baseline needs no fix.
**Production is unreachable from this container** (proxy `403`), so nothing here is confirmed
against the live site. **One premise to correct**: there is no `0209` or `0210` — the chain stops
at `0208_ideacad_materials.sql`.

## 0. 37 standing agent branches, 35 of them conflicting, so a red Integrate run says nothing

Every `claude/**` and `codex/**` ref put to `git merge-tree --write-tree` against
`origin/integration`: **52 agent branches, 15 already contained, 37 standing, 35 of those 37
conflicting.** Three consecutive Integrate runs (`34721758677`, `34721804705`, `34721937193`)
ended `failure` in under 40s each on eleven notebook-theme files —
`claude/notebook-ui-theme-overhaul-0gnx0f`, which `CLAUDE.md` records as Mr. Pina's to delete.

The cost is the ledger `README`'s claim that a standing branch is a **signal** (live session, red
CI, conflicted merge): at 37 it reads as none of those, and **the 15 contained branches should
already be gone**.

**CORRECTION, measured after the above was written.** This audit's own branch was then swept, and
run `34723007701` shows the redness is deliberate and self-describing: `1 branch(es) conflicted
... integration ITSELF WAS PUSHED and is not blocked -- this run is red for that reason as well
as for anything the suite found`. A red run is not information-free, and the remedy is deleting
one branch, not 35. **The same run proves `merged_suite` works**: `the merged tree passes the
suite -- Tests 8318 passed (8318)`. Ledger 0163's fix landed, so the signal decision 21's gate
would read is green — which unblocks item 1 rather than excusing it.

---

## 1. A decision Mr. Pina already answered is not built, and no tool lists it

`docs/decisions/entries/21-integrate-tests-after-it-merges.md` reads
`Status: decided 2026-09-12. YES, BLOCK` with `- Build: OPEN`. The workflow still does what he
said to stop: `.github/workflows/integrate.yml:16` pushes `integration` and deletes the source
branch with `merged_suite` running afterwards, so **a red merged tree still cannot stop a merge
or a delete** — how run 34363724495 took a branch in, deleted it, then failed
(`docs/history/measure-the-nine-and-land.md:177`).

**The tracking hole is the real finding.** `tools/idea-status.py:188` filters `Status: open` and
never reads `Build:`, so a decided-but-unbuilt decision is in no list. Two are, and **one is
already stale**: `04-gallery-default-most-played.md:8` says `Build: OPEN` and names
`FoundryGallery.svelte` line 127, but `src/lib/foundry/telemetry.ts:192` already reads
`FOUNDRY_GALLERY_DEFAULT_SORT = 'played'`.

## 2. The applied record is empty, and the tool that would fill it cannot run

`docs/migrations-applied/` holds `README.md` and nothing else — **0 records for 16 migrations**.
The writer is not missing (`tools/apply-migration.mjs:122`, `:1644`); it has never run because
every migration here was pasted by hand and a session has no `IDEA_MIGRATION_URL` — each file
says so ("DO NOT APPLY FROM THE SESSION CONTAINER").

**What it costs**: nothing here can answer *what is live*. `0206` exists only because `0202` is
applied and cannot be rewritten — a judgement needing an applied record there is none of. Ledger
0188's sentence that `0205`-`0208` were applied 2026-09-13 is the whole record. **What would
close it**: move the apply to the tool (needs `IDEA_MIGRATION_URL` reachable from a session), or
have whoever pastes commit one record per paste.

## 3. Every production page has stamped `local build` since 2026-09-09

`src/lib/site-versions.ts:291` is `deploy.date || 'local build'`; `vite.config.ts:81` still runs
`git log --no-merges`. `main` advances by `--no-ff` merges, so `VERCEL_GIT_COMMIT_SHA` names a
commit the log excludes, `agrees` is false and the date is `''`. Diagnosed at
`docs/history/migration-0192-verification-0dqhto.md:272`; still true.

## 4. A live student-facing data defect, reported and never picked up

`src/lib/legacy/assignments/idea113-blade-01.html:1463` and `:1472` identify an uploaded image
**by its bytes** (`images.findIndex(i => i.dataUrl === dataUrl)`). Upload one photo twice and the
second caption writes into the first entry; delete removes the wrong one
(`docs/history/idea113-export-doubling-fct5fc.md:205`). Its companion claim — `-03`/`-04`
doubling every photo in the export — is a runtime property, **unverified here**.

## 5. `0208` is applied and no student can reach it

`BladeEditor` takes `materials` and `saveCustomMaterial` as optional props and `grep` finds
neither in `mount.ts`, `transports.ts` or the item route. The harness and
`/admin/ideacad-materials` supply them; the real classroom page does not
(`docs/history/vibrant-brown-mgym02.md:241`).

## 6. The migration chain (question 3): one real overlap, and it is sound

Only **seven objects** are touched by more than one migration in `0193`-`0208`:
`ideacad_new_concept`, `save_concept`, `update_concept_meta`, `delete_concept`, `set_active`,
`set_prediction`, `commit_concept` — created by `0201`, replaced by `0205`. **Not a
contradiction**: all seven parameter lists are identical, as `0205:433` claims, so the signature
trap does not apply and `0202`'s repaired ACL survives. No table or policy is named twice.

**One genuine defect**: `0202` is no longer re-appliable. Its section-4 sweep asserts ten
ideacad functions; there are nineteen, so a re-paste raises twice —
`0206_ideacad_grant_guard.sql:43-46` quotes both. `0206` documents and does not repair it,
against `CLAUDE.md`'s "Test that the file re-applies", recording the exception **only in a
migration comment**.

`0201`'s broken `revoke ... from public` shape is repaired by `0202`, `0203` closes the sequence
half, and `0205`/`0207`/`0208` use `0166`'s role-naming form — multi-line, so a single-line grep
reports them broken when they are not.

## 7. Vacuous tests (question 2): **nothing found**

No seventh found. No `.skip`, `.todo` or `skipIf` in `tests/`; no `git checkout --` in any test
or tool (the five hits are comments citing the rule); `tools/run-tests.mjs` pairs `--outputFile`
with `--reporter=json` and fails closed with no report; the one `existsSync` early-return
(`tests/foundry-bundle-url.test.ts:393`) sits above a `files.length > 100` control. The sweeps
carry case-count floors — `classroom-spec-text-surfaces.test.ts` (`specs.length >= 10`,
`checked > 500`), `grant-surface.test.ts:655`, eleven in
`ideacad-grants-anon-execute-surface.test.ts`, and
`tests/db/grant-sequence-sweep-control.test.ts` for sequences.

**One narrow gap**: `tests/foundry-bundle-url.test.ts:405` walks `src/` recursively but reads
`tests/` **top level only**, so a retired Foundry name under `tests/db/**` escapes it.

## 8. Already shipped — eight stale open items, listed so nobody re-opens them

`student_app_plays`' `service_role` grant (`0204:396`); sequence grants (`0203`); the
`svelte-check` baseline (now agrees, including `CLAUDE.md:2437`); the `/hx` origin-split rule;
the HTML-import harness (`/dev/html-instructor`); `claude/inspiring-planck-gp601z` (contained);
`foundry_my_play_stats` (`0204`); decision 04. **Eight open, eight already done** — which is why
each was checked rather than copied forward.

## 9. Small and cheap

- **`CLAUDE.md:45` omits `0186_maps_media_no_anon_listing.sql`** from the IDEA Maps list — the
  migration closing anon listing on a public bucket, so the omission is security-relevant.
  (`docs-standards-ledger-hihkhd.md:279` also claims `0164` is missing; it is not.)
- **`.env.example:163`** still says isolation comes from "the iframe sandbox with no
  `allow-same-origin`", contradicting the conditional-grant rule.
- **`RubricBuilder.svelte:595-596`** — 28px reorder arrows, no instructor-only class, so 44px
  applies. This one *is* tracked, in open decision 13.

## 10. Question 5: what exists only as prose

Items 2, 3, 4, 5 and the first two bullets of 9 live in one history entry each, in no decision
entry, no ledger line and no `CLAUDE.md` rule; the `0202` exception only in a migration comment.
The cause is item 1: a history entry is a **dated record never edited**, so an item parked there
has no state and no reader, where a decision entry has a `Status` a tool prints. **The cheapest
fix is to teach that tool to read `Build:`** — it would have surfaced item 1 and stale decision
04 without anybody reading 75 files.
