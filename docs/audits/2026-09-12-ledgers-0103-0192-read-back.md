# Read-back audit: ledgers 0103 through 0192

- Date: 2026-09-12
- Bundle: ledger 0193, branch `claude/gallant-mendel-xsgp0w`, from `origin/integration` at `7f5ca8f0`
- Window: 75 `docs/history/` entries dated 2026-09-09 to 2026-09-12; migrations `0193`-`0208`; 89 new test files
- Changes nothing. Every item was re-checked against the tree before listing.

**Tree green as found**: `npm test` **433 files, 8318 tests, 0 failures** (exit 0, 453.9s);
`svelte-check` **0 errors, 37 warnings in 20 files (31/5/1)**, so the `CLAUDE.md` baseline is
correct and there is no line to fix. **Production is unreachable from this container**
(`ideabosco.com` and the Vercel host both `CONNECT tunnel failed, response 403`, code `000`),
so nothing here is confirmed against the live site.

**One premise to correct**: there is no `0209` or `0210`. The chain stops at
`0208_ideacad_materials.sql`; `tools/migration-claims.mjs` reports `next free 0209`.

---

## 1. A decision Mr. Pina already answered is not built, and no tool lists it

`docs/decisions/entries/21-integrate-tests-after-it-merges.md` reads
`Status: decided 2026-09-12. YES, BLOCK` with `- Build: OPEN`. The workflow still does what he
said to stop: `.github/workflows/integrate.yml:16` pushes `integration` and deletes the source
branch, with `merged_suite` running afterwards. **A red merged tree still cannot stop a merge
or a delete** — how run 34363724495 took a branch in, deleted it, then failed
(`docs/history/measure-the-nine-and-land.md:177`).

**The tracking hole is the real finding.** `tools/idea-status.py:188` filters
`Status: open` and never reads `Build:`, so a decided-but-unbuilt decision appears in no list.
Two entries are in that state and **one is already stale**:
`04-gallery-default-most-played.md:8` says `Build: OPEN` and names `FoundryGallery.svelte`
line 127, but `src/lib/foundry/telemetry.ts:192` already reads
`FOUNDRY_GALLERY_DEFAULT_SORT = 'played'`. It shipped; its entry does not say so.

## 2. The applied record is empty, and the tool that would fill it cannot run

`docs/migrations-applied/` holds `README.md` and nothing else — **0 records for 16
migrations**. The writer is not missing: `tools/apply-migration.mjs:122` and `:1644` build
and commit the file. It has never run because every migration in this window was pasted by
hand, and a session container has no `IDEA_MIGRATION_URL` — each file says so in its own
header ("DO NOT APPLY FROM THE SESSION CONTAINER").

**What it costs**: nothing in the repository can answer *what is live*. `0206` exists only
because `0202` is applied and its text cannot be rewritten — a judgement needing an applied
record there is none of. Ledger 0188's sentence that `0205`-`0208` were applied 2026-09-13 is
the entire record. **What would close it**: move the apply to the tool (needs
`IDEA_MIGRATION_URL` reachable from a session — a decision, not a lane), or have whoever
pastes commit one record file per paste. The second needs no change to anything.

## 3. Every production page has stamped `local build` since 2026-09-09

`src/lib/site-versions.ts:291` is `deploy.date || 'local build'`; `vite.config.ts:81` still
runs `git log --no-merges`. `main` advances by `--no-ff` merges, so `VERCEL_GIT_COMMIT_SHA`
names a commit the log excludes, `agrees` is false and the date is `''`. Diagnosed in full at
`docs/history/migration-0192-verification-0dqhto.md:272`; still true in the tree.

## 4. A live student-facing data defect, reported and never picked up

`src/lib/legacy/assignments/idea113-blade-01.html:1463` and `:1472` identify an uploaded
image **by its bytes** (`images.findIndex(i => i.dataUrl === dataUrl)`). Upload one photo
twice and the second caption writes into the first entry; delete removes the wrong one.
Measured by `docs/history/idea113-export-doubling-fct5fc.md:205`. Its companion claim —
`-03`/`-04` doubling every photo in the export — is a runtime property, **unverified here**.

## 5. `0208` is applied and no student can reach it

`BladeEditor` takes `materials` and `saveCustomMaterial` as optional props and `grep` finds
neither in `mount.ts`, `transports.ts` or the item route. The dev harness and
`/admin/ideacad-materials` supply them; the real classroom page does not
(`docs/history/vibrant-brown-mgym02.md:241`). An applied migration with no student-facing
caller is the state most likely to be forgotten.

## 6. The migration chain (question 3): one real overlap, and it is sound

Only **seven objects** are touched by more than one migration in `0193`-`0208`:
`ideacad_new_concept`, `save_concept`, `update_concept_meta`, `delete_concept`, `set_active`,
`set_prediction` and `commit_concept`, created by `0201` and replaced by `0205`. **Not a
contradiction** — I compared parameter lists and all seven are identical, which is what
`0205:433` claims, so the signature trap does not apply and `0202`'s repaired ACL survives the
replace. No table and no policy is named by two files.

**One genuine defect**: `0202` is no longer re-appliable. Its own section-4 sweep asserts
exactly ten ideacad functions; there are now nineteen, so a re-paste raises twice —
`0206_ideacad_grant_guard.sql:43-46` quotes both raises verbatim. `0206` documents this and
does not repair it, and `CLAUDE.md`'s rule is "Test that the file re-applies." The exception
is recorded **only in a migration comment**, which is the one place nobody reads.

`0201`'s broken `revoke ... from public` shape is repaired by `0202`; `0203` closes the
sequence half; `0205`, `0207` and `0208` all use `0166`'s role-naming form (multi-line —
a single-line grep misses them). That chain is coherent.

## 7. Vacuous tests (question 2): **nothing found**

I looked and did not find a seventh. No `.skip`, `.todo` or `skipIf` anywhere in `tests/`.
No `git checkout --` inside any test or tool — the five hits are comments citing the rule.
`tools/run-tests.mjs` sets `--outputFile` beside `--reporter=json` and fails closed with no
report. The one `existsSync` early-return (`tests/foundry-bundle-url.test.ts:393`) sits above
a `files.length > 100` control. The sweeps carry case-count floors:
`classroom-spec-text-surfaces.test.ts` (`specs.length >= 10`, `checked > 500`),
`grant-surface.test.ts:655`, and eleven in
`ideacad-grants-anon-execute-surface.test.ts`. Sequences now have
`tests/db/grant-sequence-sweep-control.test.ts` with a positive control.

**One narrow gap, low value**: `tests/foundry-bundle-url.test.ts:405` walks `src/`
recursively but reads `tests/` **top level only**, so a retired Foundry name reappearing under
`tests/db/**` escapes it. And `grant-surface.test.ts:655`'s floor is `>= 147` against a
206-file chain — non-vacuous, just stale.

## 8. Already shipped — eight stale open items, listed so nobody re-opens them

`student_app_plays`' `service_role` grant (closed by `0204:396`); sequence grants (`0203`);
the `svelte-check` baseline (now agrees, including at `CLAUDE.md:2437`); the `/hx`
origin-split rule; the HTML-import harness (shipped at `/dev/html-instructor`);
`claude/inspiring-planck-gp601z` (contained in `integration`); `foundry_my_play_stats`
(`0204`); decision 04. **Eight open, eight already done** — which is why each was checked
rather than copied forward.

## 9. Small and cheap

- **`CLAUDE.md:45` omits `0186_maps_media_no_anon_listing.sql`** from the IDEA Maps schema
  list — the migration closing anon listing on a public bucket, so the omission is
  security-relevant. (`docs/history/docs-standards-ledger-hihkhd.md:279` also claims `0164`
  is missing; it is not — `0161 through 0165` covers it.)
- **`.env.example:163`** still says isolation comes from "the iframe sandbox with no
  `allow-same-origin`", which `CLAUDE.md`'s conditional-grant rule contradicts directly.
  Flagged as a deliberate non-edit by `docs/history/reserve-hx-slug-8zq09j.md:188`.
- **`RubricBuilder.svelte:595-596`** — 28px reorder arrows, no instructor-only class, so the
  44px floor applies. This one *is* tracked, in open decision 13.
- **`tests/gauntlet-practice-meter.test.ts`** is named for `gauntlet_practice_meter`, which
  `CLAUDE.md` calls fiction. Its contents correctly name `gauntlet_practice_pressure`; only
  the filename is wrong.

## 10. Question 5: what exists only as prose

Items 2, 3, 4, 5 and the first two bullets of 9 live in exactly one history entry each, in no
decision entry, no ledger line and no `CLAUDE.md` rule; the `0202` re-apply exception lives
only in a migration comment. The structural cause is item 1: a history entry is a **dated
record that is never edited**, so an item parked in one has no state and no tool reads it,
where a decision entry has a `Status` that `tools/idea-status.py` prints. **The cheapest fix
is to teach that tool to read `Build:`** — it would have surfaced item 1 and stale decision 04
without anybody reading 75 files.
