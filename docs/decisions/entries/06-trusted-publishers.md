# 06 Foundry: trusted publishers
- Raised: 2026-08-31  By: chat "Managing multiple FRC platform projects"
- Status: decided 2026-09-02; shipped the same day by prompt 0015 (`0173_foundry_section_gate_description_and_trust.sql` section 3). These two lines were flipped on 2026-09-07 by prompt 0099, five days after the fact.
- Decision: An admin marks a student trusted (`foundry_trusted_grant`, an allowlist keyed on lowercased email that mirrors `app_admins` and `gauntlet_authors`); a trusted student's publish goes live immediately, in the same transaction as `foundry_submit_version`, and the review queue shows it as ALREADY LIVE, reviewed after the fact rather than before. That is the default this entry proposed, and it is the answer Mr. Pina gave on 2026-09-02, recorded in ledger 0015.
- Default this assistant would pick: Reviewed after the fact, with the review queue showing a trusted publisher's update as already live.
- Why it is blocked on him: It decides which students' work reaches other students without a staff read first, which is a supervision call.
- What it unblocks: A Foundry lane modelled on the two existing allowlist tiers.
- Context: migrations `0155_gauntlet_authoring_tier.sql` (`gauntlet_authors`, `gauntlet_can_author()`) and `0167_frc_reviewer_tier.sql` (`frc_reviewers`) are the template; `CLAUDE.md`, "GAUNTLET AUTHOR TIER" for the allowlist shape. The open question is whether a trusted publisher's update is unreviewed or reviewed after the fact.
- Tree check (2026-09-02): both template migrations exist on `origin/main`; no publisher tier exists for Foundry.
- Tree check (2026-09-07): shipped by `0173_foundry_section_gate_description_and_trust.sql` section 3 (`foundry_trusted_publishers`, `foundry_is_trusted()`; a trusted student's `foundry_submit_version` publishes in the same transaction and the review queue lists it as live, not yet reviewed), and ledger 0015 records the answer. This entry's Status and Decision lines above were not updated by any bundle and are the owner's to flip; prompt 0098 left them as found.
- Transition (2026-09-07, prompt 0099): Status `open` -> `decided 2026-09-02`, and the Decision line filled from ledger 0015 and 0173 section 3. Only those two lines moved; both tree checks above stand as written. Four sibling entries answered in the same ledger and shipped by the same bundle still read `open` with an empty Decision line -- 01 (0173 section 1, the per-section gate), 03 (the launcher card rule and the rewritten rationale in `tests/home-order-and-accent.test.ts`), 05 (0173 section 2, the required description) and 07 (`FoundryPlayStats`, the owner dashboard) -- and 17 reads `open -> ANSWERED` with its Decision line still empty. Prompt 0099 owned this entry alone and reports the other five rather than flipping them.
