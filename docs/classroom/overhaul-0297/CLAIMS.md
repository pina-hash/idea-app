# Claims in the brief that are not plainly true, ledger 0297

Every statement in `OVERHAUL_0297.md` about the repository was treated as a claim and checked against the tree at `33849402` by the Phase 0 auditors. 142 were checked: 84 true, 30 partly true, 7 false, 2 unverifiable from this container. The 39 that are not plainly true follow, with the evidence each auditor gave. The history entry for this run carries the summary.

- **partly**: People, Grades, Duplicates and the class list cap at 60rem (about 240px of margin each side) Evidence: People/Grades measured 960px at x=240 (1440); Duplicates reads var(--cr-measure,92rem) with route measure page (duplicates/+page.svelte:50). The class list does NOT cap: in the real split with nothing open .cr-nav spans 1376px at 1440 (split.css:473 --cr-nav-w:100%, nav --cr-measure:none).
- **false**: Panes are bounded by 100vh - --cr-chrome-h (10.5rem, split.css) Evidence: split.css:505-512 says nothing resolves it any more; .cr-root > .cr-split:not(.page-flow) > .cr-detail sets max-height:none, height:100% (split.css:615-623); measured computed max-height 'none'. The room is 100dvh and the chrome measures itself (split.css:264-285).
- **partly**: Home hero styles in src/app.css .legacy-index carry about 122 color literals and are hardcoded dark Evidence: 122 is the count for ALL of src/app.css (grep and counter agree). The .legacy-index block (app.css:1990-3142) holds 59 rgba() literals (0 hex) plus 13 color-mix() over tokens; hardcoded dark: yes, header bg rgba(19,26,19,0.88) at app.css:2031.
- **false**: IdeaCadMark (648e3152) and docs/ideacad/VISION.md live on claude/gracious-ride-cdf7jw until 0296 lands Evidence: Both are on this tree: src/lib/marks/IdeaCadMark.svelte last touched by 648e3152 and docs/ideacad/VISION.md exists.
- **partly**: Accessibility: 44px student floor Evidence: Classroom student surfaces mostly clear (54 controls, only the crumb reach at 43.4px), but the home page fails at every width (class chip 26.9, tour 23.6, select 24.6, launcher buttons 22.6) and the switcher's .sw-item is 40px.
- **partly**: F2.2: IDEA_INTERFACE_STANDARDS.md 2.12 requires a named class for the 24px instructor floor Evidence: 2.12 is a changelog version (IDEA_INTERFACE_STANDARDS.md:264); the rule is section 10. Today the floor is claimed by .btn.tiny (24px) inside .cr-root (ItemDetail.svelte:1575-1580), not by a class on a surface root.
- **partly**: Nothing inside /classroom shows to-do or due work Evidence: True for assignments: src/routes/classroom/+page.svelte mounts MyClasses.svelte (no counts) and ClassView has no due axis. But ClassView shows notebook check-in status 'Not filed yet' (class-check-ins.ts:296) and an outstanding count badge beside 'My notebook' (ClassView.svelte:394-396, 1549-1556).
- **partly**: planDeck requires one root-level entry page and refuses otherwise (report 23) Evidence: src/lib/server/classroom-decks.ts:394-470: one root HTML is picked; several candidates return 'Choose which one to open' with candidates (uploader picks, preferredEntry); zero root HTML is refused ('That zip has no HTML file at its top level'). An image-only zip is refused.
- **partly**: src/lib/notebook/PhotoViewer.svelte is a lightbox with zoom and fit but no download Evidence: Zoom in/out/fit buttons at PhotoViewer.svelte:365-369 and no download are true, but it is notebook-bound (imports PhotoPage, photoSrc, driveOpenUrl at :31, --nb-* palette, 'Open in Drive' only on a broken photo :315), so it is not a drop-in classroom lightbox.
- **partly**: Item page autosaves on an 800ms debounce mirrored to localStorage Evidence: True for v1: AssignmentEngine + src/lib/classroom/assignment-draft-mirror.ts, save-state.svelte.ts:130 debounceMs 800. v3 HTML: html-assignment/answers.ts:921 debounce 800 but no localStorage mirror anywhere in html-assignment/.
- **unverifiable**: Only fullscreen in the classroom is DeckViewer Evidence: Not audited outside my slice; within my slice only DeckViewer.svelte calls requestFullscreen.
- **false**: At least five taps per photographed page Evidence: Measured minimum is 3 on the Android in-app camera (Take a photo, shutter, Skip; driven with a fake camera) and 4 on the native camera or gallery path (Take a photo, OS shutter, OS confirm, Skip). Five or more only when the student drags corners and presses Flatten.
- **false**: Desktop requires 'New entry' first Evidence: NotebookView.svelte:434 sets composing = $state(true), and :453 mounts the composer whenever !readOnly && (!wide || composing). The composer measured open on load at 1366 and 1440 (shots/task-1366-c-nb-open.png). 'New entry' is only needed after opening an entry, which hides the composer via class:behind (:2950).
- **partly**: Flags are reason codes; feedback is a verdict, not a next step Evidence: There are five reason codes (notebook.ts:359-365) plus an optional one-line free-text comment up to 2000 characters (EntryReview.svelte:509-523), which the student sees under the flag. Accept writes only reviewed_by and reviewed_at and takes no comment (EntryReview.svelte:37-48).
- **partly**: IDEA_MATERIALS_PROCESS.md 'Notebook Work Attaches To An Assignment' routes paper notebook work into assignment imageZones Evidence: The section (line 420) says a notebook entry is 'one or more modules inside the assignment', that the assignment holds 'the photograph of the paper', and that Documentation Checks stay separate. It never names imageZone, which is only the implied mechanism (line 49: bench recording 'photographed and uploaded').
- **partly**: NotebookEntryCard and EntryReview implement the entry frame twice Evidence: The header, status chips, stamp and flag callout are hand-written in each file (1949 and 883 lines). The photos (NotebookPhotos) and notes (EntryNotes) are shared components. Rebuild plan 4c-2 still lists the merge as owed (IDEA_CLASSROOM_REBUILD_PLAN.md:156).
- **partly**: The data model allows one-step capture filed to (class, today, item title) without a migration Evidence: Creation works today: notebook_create_entry and notebook_create_note_entry accept p_section_id with no session and no enrollment check (0118:122-215), and p_submitted=false makes a draft that notebook_add_photo keeps accepting. Missing: an entry has no item key (only its text label), the date is the server's upload_timestamp, and photoPages (notebook.ts:558-579) pairs a corrected photo with whichever original precedes it, so correcting after the fact mis-pairs.
- **partly**: There is no needs-attention queue across classes Evidence: Nothing exists inside /classroom (MyClasses.svelte has no counts). But the site home feed shows per class 'N need attention' and per item 'N to grade' (feed.ts:50 caps it at 6 rows; isAwaitingGrade at feed.ts:201 counts resubmissions without labeling them; the row links to the item, ClassroomFeed.svelte:86-87). The Grades tab shows 'waiting to be marked' and 'resubmitted after grading' (GradesPanel.svelte:93-176), and /classroom/grading/[itemId] grades one item across classes. No notebook entries appear in any of these.
- **partly**: There are no hidden grading standards Evidence: 0090 instructor-only attachments and links exist (classroom_instructor_attachments and classroom_instructor_resources, read at transports.ts:818-823), so a standards document can be stored today. GradingConsole never shows them, so they are not in reach while grading.
- **partly**: Teams shipped in 0293 / migration 0223 (roster, CSV import, random picker, teams persist/export/post) Evidence: The teacher side exists: PeoplePanel.svelte, teams.ts (createTeamTransports :289), picker.ts, 0223_classroom_teams.sql. The posted roster has no student surface (ClassView.svelte has no team code; CLAUDE.md says the student surface is not built), yet the post acknowledgement claims the class can see it. Whether 0223 is applied in production is unknowable from here. Roster, CSV import and the picker predate 0293.
- **partly**: nothing handles a deploy landing under an open page Evidence: No app code: grep finds no updated import from $app/state, no vite:preloadError, no preloadCode, no src/hooks.client.ts. But SvelteKit itself calls updated.check() when a route/data load throws a non-HTTP error and does a native reload of the target if the version changed (kit runtime/client/client.js:1338-1345, 1820-1825). In dev check() is hard-wired false (runtime/client/utils.js:252-258), so dev shows the error page (measured E3).
- **partly**: one [export commit] lands each time a teacher saves an item Evidence: classroom-export.ts skips with no token (:431), with no spec (:441, :461) and commits nothing for an identical tree (:355). origin/main carries only 2 materials/ commits since 2026-09-13 (66cbe23b 09-14, 966cb444 09-18), against 105 first-parent commits on main in that window, each also a deploy.
- **partly**: Autosave and local mirrors for assignments and notebook text Evidence: Autosave via SaveState everywhere. Mirrors exist for v1 spec assignments (assignment-draft-mirror.ts, AssignmentEngine.svelte:251), notebook notes (notebook/draft-mirror.ts) and maps shelf. None for v3 ported HTML assignments (html-assignment/answers.ts: one SaveState per block, no localStorage), none for ContentComposer drafts, none for the instructor copy.
- **partly**: Item page: autosave on an 800ms debounce mirrored to localStorage Evidence: True for v1 spec assignments only (AssignmentEngine); v3 HTML assignments autosave per block with no localStorage mirror (answers.ts, answers-store.svelte.ts).
- **partly**: Whether Vercel's skew protection is enabled is a dashboard setting a container cannot read Evidence: adapter-vercel 6.3.4 sets Set-Cookie __vdpl=<deployment id> on Sec-Fetch-Dest: document responses only when built with VERCEL_SKEW_PROTECTION_ENABLED (node_modules/@sveltejs/adapter-vercel/index.js:643-656), so one curl from a machine that reaches production answers it. This container cannot: the agent proxy answered 403 to CONNECT ideabosco.com (measured).
- **partly**: The only fullscreen is DeckViewer Evidence: True inside the classroom (DeckViewer.svelte:66-78 fullscreens .deck-stage). Site-wide also foundry/AppStage.svelte, AppFrame.svelte, fsp/FspDeck.svelte, FspDayArchive.svelte, FspStudentPreview.svelte, routes/fsp/live/+page.svelte, tournaments/fullscreen.ts.
- **false**: A lazy-loaded chunk that fails after a deploy gives a white screen (implied by F6.2) Evidence: Measured: a failed route chunk renders the root +error.svelte inside the app chrome (kit loads the root layout and error nodes at start); a failed tiptap chunk leaves a dead editor, not a white page. The real losses are state (composer destroyed) and a dead input.
- **false**: (task question) dev mode polls /_app/version.json Evidence: Measured: 0 version.json requests over 20s idle on /dev/classroom-split/s-1; the dev server answers /_app/version.json with 404; updated.current false and updated.check() resolves false in dev.
- **partly**: tests/identity-style-shared.test.ts fails on main permanently: its 'exactly two colors moved' case reads git show origin/main:src/lib/profile.ts and its premise expired when bf04a223 merged, as recorded in docs/history/new-session-avpli9.md. Evidence: True at bf04a223 (git show bf04a223:tests/identity-style-shared.test.ts:209 reads 'origin/main:src/lib/profile.ts'). No longer true: commit 82721563 (2026-09-23 12:37 UTC, on origin/main and origin/integration) pins PRESETS_BEFORE_REF = 'f9d43b49^' (test:216); measured now `npx vitest run tests/identity-style-shared.test.ts`: 'Tests 28 passed (28)', stderr empty. The record is docs/history/gracious-ride-cdf7jw.md:296, not new-session-avpli9.md (no mention there). It is not a baseline failure on the current tree.
- **partly**: Search row: shortcuts only in the grading console and the notebook review console. Evidence: Those are the only multi-action shortcut layers with legends (GradingConsole.svelte:1566-1594, notebook-review.ts:469-484, sharing src/lib/shell/keys.ts). Single-purpose keys also exist: SpotlightTour Esc/Enter/arrows (:113-124), ReferenceDoc tablist arrows/Home/End (:548-570), sort-drag ArrowUp/Down reorder on a focused handle (sort-drag.ts:410-429), PhotoViewer arrows (:242-249), deck-stage.js arrows in the deck iframe, Tiptap Ctrl+B/I. IdeaCAD has a full remappable layer (src/lib/ideacad/solid/command-registry.ts).
- **partly**: Search row: no palette. Evidence: None in classroom or notebook (Ctrl+K/Cmd+K// DOM delta 0 on 5 views). IdeaCAD (0296) ships one: src/lib/ideacad/solid/CommandSearch.svelte over command-registry.ts searchCommands.
- **partly**: Customization row: theme per browser, nav collapse, unit folds; no density, layout or default-view preferences. Evidence: True for the classroom. Site-wide there is more: launcher layout and density (homepage {pinned, order, sort, compact default true}, portal-apps.ts:322-345), home feed folds (feed.ts:456), per-viewer disclosure state (disclosure.ts:53), profile identity style (pathway, picture, accent, badge, banner, effect, tagline; ProfileMenu labels measured), notebook plate and writing aid (per browser).
- **partly**: Learnability row: InfoTips and a spotlight tour (SpotlightTour.svelte, harness /dev/tour). Evidence: Both exist, but InfoTip has 2 call sites (AiLevelBadge.svelte, SpecRenderer.svelte) and the tour is home-only (HomeTour mounted only at src/routes/+page.svelte:661; orientation.ts has 1 sign-in + 7 home steps, measured 7 of 7 at 1440 and 375, none in classroom or notebook).
- **partly**: VISION: docs/ideacad/VISION.md is on claude/gracious-ride-cdf7jw until ledger 0296 lands on main. Evidence: 0296 has landed: docs/ideacad/VISION.md is on origin/main (last commit eff5c9e7); the ledger entry reads Status: pushed.
- **false**: Section 7: 0226 is held by ledger 0296. Evidence: 0296 claimed 0226 for proposed preferences SQL, wrote none, and released it (docs/history/gracious-ride-cdf7jw.md:54-57, 'None was written, and 0226 is released').
- **partly**: F2/Craft: IDEA_INTERFACE_STANDARDS.md 2.12 requires the 24px floor declared by a named class and student 44px targets. Evidence: 2.12 is the document's version (header line 2), not a section; the rule is section 10 (line 187), which the 2.12 changelog entry (line 264) amended. 0296's history item 6 recorded the same citation error.
- **unverifiable**: Section 9: production reachability differs per container; production served v1.1789 · bf04a22. Evidence: From this container curl to https://ideabosco.com/ fails: the egress proxy answers 403 to CONNECT (policy denial, 2026-09-23 16:27 UTC). The served version cannot be read here.
- **partly**: Section 5 Grading: the comment bank was blocked on a migration; check whether the preferences store can hold it without one. Evidence: 0219 was set aside for a comment bank table and released unused (docs/history/new-session-cf42g6.md:426-431). A per-teacher bank fits preferences with no migration, but it ships in every page's data (profile.ts:65 select includes preferences, loaded by the root layout), cannot be shared between Mr. Pina and Mr. Cosso, and must use a read-then-merge write.
- **partly**: Computed task note: src/lib/foundry/search.ts is a client-side search module that may be reusable. Evidence: foundrySearchTokens (:112) and withinOneTypo (:148) are generic and the module imports only a type module plus foundryAuthorName; foundrySearchScore (:249) and foundrySearch (:299) are Foundry-shaped. Its any-token rule differs from the notebook's every-term rule (notebook-folders.ts:198).
## Task runs from nothing

| Task | Persona | Width | Actions |
|---|---|---|---|
| Reach another class from an open item | Student | 1440 and 375 | 2 (open switcher, pick class); 3 to an item in that class, more if its unit is folded |
| Reach a notebook entry from an open item | Student | 1440 | 2 (My notebook link in the nav pane header at y=239, then an entry) |
| Reach a notebook entry from an open item | Student | 375 | 3 (crumb back to the class, My notebook, entry) |
| Put the classroom on a projector-friendly theme | Mr. Pina at the front | 1440x900 | 3 (open profile menu, scroll the page, pick Matrix) |
| Find the way back from a deck | Student | n/a | not driven (the deck harness needs an upload first) |
| See today's classes from the home page | Student | 1366x768 and 375 | 1 scroll at 1366 (Your Classes at y=835); about 2 screens of scrolling at 375 (y=1340) |
| S1 find what is due today across all classes and what is missing | student | 1366x768 | From the site home (real +page.svelte via /dev/home-order?role=student&classes=3&rows=4&due=-2,0,1,5): 1 navigation + 2 scrolls = 3 actions. First class card at y=917 (below the fold), last card ends y=2061. Inside /classroom the task cannot be done; nearest w |
| S1 find what is due today across all classes and what is missing | student | 375x812 | 1 navigation + 4 scrolls = 5 actions; feed runs y=1422 to y=2969, each class card 502px. |
| S3 find the rubric for an assignment before starting it | student | 1366x768 | v1 spec assignment: 1 click to open the item + 6 scrolls to 'How this will be graded' at y~4884 of 5994px = 7 actions (2 with the End key if known). v3 ported HTML: cannot be done in parent chrome; nearest workaround is scrolling into the framed document and o |
| S3 find the rubric for an assignment before starting it | student | 375x812 | v1: 1 + about 6 scrolls (rubric ~5.4 phone screens down) = 7 actions. v3: not possible outside the document. |
| S4 on a phone, see that the teacher returned a grade and read the feedback | student | 375x812 | Returned for real in the grading harness, then read as the student at 375. v1: home (1) + 2 scrolls to the feed + tap the 'Returned' row (1) + 1 scroll to 'Returned: 6 / 20 pts' + 2 scrolls through the 1235px rubric breakdown to 'Teacher comment' = about 7 act |
| S2: open today's assignment, photograph two notebook pages for it, write two sentences abo | IDEA209H student | 375x812 | 12 taps on iOS with the native camera, counted from the class page (13 from home): item 1, 'Open your notebook' 1, then per page 4 (Take a photo, OS shutter, OS Use Photo, corrector Skip) x2, editor 1, Turn in 1. Plus about 4 swipes: the photo controls are 103 |
| S2: open today's assignment, photograph two notebook pages for it, write two sentences abo | IDEA209H student | 1366x768 | 10 clicks, 2 pane scrolls and typing: item 1, 'Open your notebook' 1 (at y=460), then the composer is already open (no 'New entry' needed), scroll the detail pane about 300px to the photo buttons (y=1015), 'Choose a photo' 1, OS dialog (select 2 files: 2, Open |
| T1 projector agenda + 10-minute timer + who has not opened / idle + one hall pass, nothing | Mr. Pina | 1440x900 and 1366x768 | This cannot be done. No agenda, clock or timer exists. Workaround: presence takes 2 actions (open the item, press Grade in the instructor tools header) and shows presence lines under roster names. The hall pass takes 4 (trigger, open select, choose, Send). An  |
| T1 projector agenda + timer + presence + hall pass | Mr. Cosso | 1440x900 | The hall pass is found unaided (4 actions; its trigger is the first thing in the class pane). Presence is not found: nothing on the class page points at the grading roster. Timer and agenda are impossible. |
| T2 post same material to all three IDEA209H sections, scheduled tomorrow, with a notebook  | Mr. Pina | 1440x900 and 1366x768 | 12 clicks plus 5 typed fields (New post, kind, title, 2 more section checkboxes, schedule, Add a check-in, Unit, Day, Name, Attach, Schedule). Every click landed on its first attempt. Measured across /dev/classroom?view=class-teacher (two sections, no check-in |
| T2 post to three IDEA209H sections with a check-in | Mr. Cosso | 1440x900 | About 10 clicks plus 5 typed fields to post to his own Block 4 section. 'Post to' lists only sections he manages, so all three sections is not his to do. |
| T3 grade twenty submissions of one HTML assignment, then the same day's notebook entries,  | Mr. Pina | 1440x900 (layout also measured at 1366x768 and 960x900) | 1 click opens the first submitted student. Per student: 4 level keys, 3 ArrowDown, R R and N, which is 10 keys with no menu. A 25-row roster with 20 hand-ins costs 5 extra N presses. The notebook half takes 3 navigations (class crumb, 'Check-ins ›' tab into /n |
| T3 grade a class set then notebook entries | Mr. Cosso | 1440x900 | Same key count. The key legend is printed above the rubric, and the labeled Previous/Next/Save draft/Return buttons make the loop discoverable without keys (about 6 clicks per student with the mouse: 4 levels, Return, Next). |
| T4 find every ungraded or resubmitted piece of work across every class | Mr. Pina | 1440x900 | There is no single place. On site home: 1 click to home, scan one card per class ('N to grade', at most 6 rows each), then 2 more clicks per item to reach the console. Resubmissions: class switcher, pick class and Grades tab per section, about 3 actions each,  |
| T4 find all ungraded or resubmitted work | Mr. Cosso | 1440x900 | He finds the Grades tab for his one section (2 actions) and its 'to mark' and 'resubmitted after grading' chips. |
| Keep working after a save request hangs: type an answer and click to the next page | Student in IDEA209H on school wifi | 1440 | 2 (type the answer, click an in-app link) |
| Open another item in the class with a half-written post open, just after a deploy (route c | Mr. Pina (teacher) | 1440 | 3 (open composer, type title, click an item in the list) |
| Write the body of a post when the editor chunk failed to load | Mr. Cosso (teacher, no Claude access) | 1440 and 375 | Cannot be done: 0 editable elements; nearest workaround is 1 reload before typing anything, or putting the text in the Title field |
| Grade a class set by keyboard in the grading console (4-criterion rubric) | Mr. Pina at his desk | 1440x900 | Click first roster row (1 click), then per student 4 level keys + 3 ArrowDown + S + N = 9 keys; 5 students in 45 keys, 10.7s scripted. R R then N also returns and advances. |
| Discover that grading has shortcuts, never shown | Mr. Cosso | 1440, 960, 375 | Open grading console (1), open a student (1 click); legend then visible at 1440 (top 375px) and 960 (top 400px); at 375 it is 4,125px down. |
| Find an item by name in a class | Teacher or student | 1440 | No in-product search: 0 inputs. Workaround browser Ctrl+F (1 key + typing). |
| Switch to another theme while projecting from a class page | Mr. Pina at the front | 1366x768 | Open profile menu (1 tap), scroll about 520px to Theme label at y=1286 (1 scroll), tap theme (1) = 3 actions minimum. |
| Search my own notebook for an entry | IDEA209H student | 1440, 960, 375 | Type in 'Search titles and notes' (1 focus + typing): 'gearbox' 3 of 9, 'gear build' 1 of 9, 'bearing teardown' 1 of 9. |
| Open a command palette (Ctrl+K / Cmd+K / '/') | Any user | 1440 | 3 key presses on class, item, people, grading console and notebook review. |
| Fold a class card on home, then pin (or open) an app | IDEA100 student | 1440 | 1 click on the class card toggle, Customize (1), pin IDEA Maps (1). |
| Take the first-time tour and learn the classroom | First-time student | 1440, 375 | Tour auto-launches on home; 7 steps with Next/ArrowRight. |