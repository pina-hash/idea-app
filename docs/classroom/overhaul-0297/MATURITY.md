# Maturity ledger, ledger 0297

Scored 0 to 4 against the rubric in `OVERHAUL_0297.md` section 3. Every re-score is logged below with its evidence, newest last. The end state is judged by the LOWEST score, not the highest.

## Phase 0 (2026-09-23, tree `33849402`)

Measured by the six Phase 0 auditors; the router's claimed score is kept beside it for the record. Evidence cites a file, a measured behavior, or a screenshot under the Phase 0 scratch directory (listed in `AUDIT.md`).

| Area | Router claim | Phase 0 | Evidence |
|---|---|---|---|
| Live class and the teacher at the front | 1 | **1** | T1 cannot be done. No projector view, agenda, clock or timer exists anywhere under /classroom (grep finds none; the only classroom fullscreen is DeckViewer.svelte:72). The hall pass and song queue work, and sending a student out takes 4 actions. Presence renders only in the grading console roster (GradingConsole.svelte:89 is the sole importer of PresenceLine), and that screen also shows grade chips, so it cannot be projected. |
| Notebook integration with the classroom | 1 | **1** | Check-ins join the stream (class-check-ins.ts) and show on the item page (ItemDetail.svelte:1990-2047), but every door leaves for /notebook: its own masthead, a DEFAULT plate picker, and a Home link that goes to '/'. My notebook shows all classes with no class filter. A free entry is saved with section_id null (NotebookView.svelte:1730,1780), so the grid's free_entries counter gets nothing. Teacher check-in rows open the whole grid. Nothing captures from an item page. VISION.md records it unused for weeks. |
| Search, command palette and shortcuts | 1 | **1** | 0 search inputs on /dev/classroom?view= class, class-teacher, item, people, home at 375/960/1366/1440; Ctrl+K, Cmd+K and / give DOM delta 0 there and on the grading and notebook review consoles. Keyboard loops exist only in GradingConsole.svelte:1566 (GRADE_KEYS) and notebook-review.ts:469 (REVIEW_KEYS), both with on-screen legends; the only search a class user owns is in the notebook (NotebookView.svelte:2549), the surface the vision says nobody opens. |
| Customization and preferences | 1 | **1** | In the classroom: unit folds (per account), nav collapse (per browser), disclosure state; no density, default view, grading defaults, pane widths, remembered sorts or reset. Theme is the last section of the profile menu (label at y=1286 in a 768px viewport, shots/profile-1440.png). Measured on /dev/tour: folding a home class card then pinning an app wrote {homepage} only and lost the fold. |
| Learnability without instruction | 2 | **1** | The only walkthrough (HomeTour, mounted only at src/routes/+page.svelte:661) has 1 sign-in + 7 home steps, none in /classroom or /notebook, none for a teacher (shots/tour-1440-step3.png). InfoTip has 2 call sites (AiLevelBadge, SpecRenderer). The classroom teaches by prose: 15 imperative instruction blocks across 20 harness states, 6 more (~130 words) in the composer, 3 identical 33-word blocks on one teacher class page (shots/class-class-teacher-1440.png), and 17 title-only tooltips on that page. |
| Shell, navigation and layout | 2 | **2** | Split works (nav 416 + detail 936 at 1440, measured), but in split mode the trail and tab bar shrink-wrap and center (.crumbs 305px at x=567, .sec-tabs 481px at x=480 over a split spanning x=32-1408) because .cr-root:has(> .cr-split) is a flex column (src/lib/shell/split.css:273) and .crumbs/.sec-tabs use margin:0 auto (ClassroomShell.svelte:494,593); chrome takes 201px (22%) at 1440 and 250px (31%) at 375; People/Grades waste 480px at 1440; fixed Voice/Report buttons win hit tests over row controls at 960 (shots/split-section-teacher-960.png). |
| Visual design and themes | 2 | **2** | Only IDEA and Matrix (src/lib/theme.ts:10); ThemeRoot writes data-theme in an $effect: FCP 192ms vs data-theme at 829ms on /dev/themes (34 unthemed frames), 344 vs 1130ms on the home page; the only switch sits at y=1018 inside a 1139px-tall profile panel at 1440x900 (shots/task-theme-menu-open-1440.png); no light theme; tests/theme-tokens.test.ts locks semantic/identity tokens and pins 7 non-token exceptions. |
| Banners, mastheads and home entry | 1 | **2** | Usable, but the home hero is 540px (70% of a 1366x768 viewport) and pushes Your Classes to y=835 at 1366 and y=1340 at 375 (shots/home-student3-1366.png, home-student3-375.png); header background is a literal rgba(19,26,19,0.88) (src/app.css:2031); logo is a raster dark-metal plate that reads as a sticker on light (shots/logo-on-grounds.png); three different mastheads; the follow-the-active-theme half is effectively 0. |
| Class stream and organization | 2 | **2** | Units in teacher order, pinned first, per-user folds all work (src/lib/classroom/classroom.ts:1125-1230); ClassView.svelte has no search, filter, sort or due-date control; an overdue unstarted assignment reads 'Not started' in the muted tone exactly like a future one (classroom.ts:1340-1351); /dev/classroom-stream at 1440 leaves a 426x702px void under a short Unit 1 beside an 903px Unit 2 (stream-1440.png). |
| Student to-do, due dates and missing work | 1 | **2** | Works only on the site home: ClassroomFeed ranks overdue, due-within-7-days and unseen returns per class (feed.ts:32, mounted src/routes/+page.svelte:531); measured first card at y=917 at 1366 and y=1422 at 375 (task-s1-*.png), capped 6 rows per class (feed.ts:50), no Missing/Done view, notebook check-ins absent, and MyClasses.svelte inside /classroom shows no counts at all. |
| Item page (student) | 3 | **2** | v1 rubric sits below Submit at y~4884 of 5994px at 1366 (s2-assignment-1366-full.png); v3 ported HTML renders no rubric, no returned score and no teacher comment although data.engine carries rubric + submission (ItemDetail.svelte:2434-2567, transports.ts:1276-1311); non-HTML content capped at 736px in a 936px pane at 1440 and in a 1376px pane with the list hidden (640px empty, item-navcollapsed-1440.png). |
| People, teams and class tools | 3 | **2** | The teacher side is built (roster, CSV import, export, email, picker, saved teams). But 'Post to the class' says 'Posted. The whole class can see these teams.' and no student surface renders posted teams (ClassView.svelte has no team code). The random picker sits at document y=3962 at 1440 (7014 at 375), and the drawn teams render at y=5947, 1831px below the Draw button at y=4116, so a draw shows nothing on screen. `.cr-root .btn.tiny` beats `.tap-44`, so 86 of 90 People controls measure 24 to 43px, with no density class on the surface root. |
| Notebook capture | 1 | **2** | Capture works end to end at 375 and 1366 (harness log: POST /api/notebook/note autosave, notebook_seal_notes, add-photo x2, notebook_submit_entry). The Android in-app camera takes 3 taps a page, driven with Chromium's fake camera. Held at 2 because photos stay in memory until Turn in (NotebookView.svelte:643), every photo is forced through PhotoCorrector, and the masthead covers the corrector's title at every width (hit test lands on .app-header; shots/corrector-1366.png). Also: no drop or paste; at 375 the photo buttons sit 1030px below the composer top; at 1366x768 no entry rows show on load. |
| Notebook review and grading | 2 | **2** | ReviewConsole has the grid, the A/F/Enter/Esc keys (notebook-review.ts:469-486), a Live realtime channel (review/+page.svelte:315), excusals, and a Documentation Check that grades through classroom_grade_submission (review/+page.svelte:509). All were driven. Gaps: at 1366x768 the grid shows 684 of 782px and hides the Covered column, while the 544px entry pane holds 122px of content (shots/review-1366.png). Accept takes no comment, a flag is a reason code plus one optional text line, and there is no approve-all. The grading console has no route in, and Grade unit is a disabled button explained only by a title tooltip. |
| Decks, media and embeds | 2 | **2** | DeckViewer plays with fullscreen, but its 58px bar overlays the bottom of the stage with Back bottom-left (DeckViewer.svelte:110-181) and at 375 the Report control runs off-screen (x=368.3 to 463.1, deck-viewer-375.png); body images fixed at 352px with no click action (1202x1202 source, 0 dialogs on click); image attachments open a Content-Disposition download instead of a view; no gallery; inline YouTube links bare (ItemBody.svelte:101). |
| Speed, reliability and deploy safety | 2 | **2** | Save machine and guard work (measured E8: a link clicked inside the 800ms debounce flushed 1 dispatched/1 acknowledged, then navigated), but svelte.config.js:5-10 sets no pollInterval (kit default 0, options.js:322) and no app code reads updated; a failed tiptap chunk leaves a 716x240 body with 0 editable elements (e2b-typed-into-failed-editor-1440.png); a failed route chunk destroyed an open composer's typed title (e3-route-chunk-failed-1440.png); a hung save left the URL unchanged for 8s with no nav indicator (e8-guard-hang-1440.png); v3 HTML assignments and the teacher composer have no local mirror; masthead mark costs 2,603,273 bytes of PNG per first load. |
| Accessibility and legibility | 3 | **2** | Home page (the student dashboard) fails 44px at every width: .class-chip 26.9px, .tour-link 23.6, .bar-select 24.6, .bar-btn 22.6; switcher .sw-item 40px; under the 300:1 + 10% ambient projector model IDEA --text-2 is 4.21:1, --dim 3.35, --crimson 3.11, --boundary 2.65 on --surface-1 and surface-0 vs surface-1 separates at 1.025; nothing in tools/ or tests/ measures projector legibility. |
| Creation and composer (teacher) | 3 | **3** | T2 completes in 12 clicks + 5 typed fields, every click landing on the first attempt (shots/t2-composer-filled-1440.png, shots/t2-checkin-staged-1440.png). Held back from 4 by report 21 still open (a .json spec and a .html document dropped on the title input are staged as general student-visible files, measured by drop.mjs), the full-viewport form capped at max-width 64rem (ContentComposer.svelte:3349-3353, 352px unused at 1440), 25px kind tabs, 24px add-link controls and 19px section labels on a surface that says it is held to 44px (:3716-3719), and a check-in whose Unit/Day/Name do not inherit the composer's unit, schedule or title. |
| Grading and feedback | 3 | **3** | Keyboard loop measured on /dev/classroom?view=grade: 4 level keys + 3 ArrowDown + R R + N = 10 keys per student, with no menu between students in one assignment (t3loop.mjs). Held back by: no carousel (grade(true) at GradingConsole.svelte:1080 keeps the student, and N walked into 'Not submitted' rows Dana Whitfield and Dara Nwosu), no comment bank, no queue across classes, a ported document 300px wide in a 562px pane at 960 (shots/htmlgrade-selected-960.png), the open/closed count clipped to 35 of 103px at every width, and about 228px of rubric visible at 1366x768. |

Lowest at Phase 0: **1** (Live class and the teacher at the front; Notebook integration with the classroom; Search, command palette and shortcuts; Customization and preferences; Learnability without instruction). Under the catch-up rule the two areas at 3 (Creation and composer; Grading and feedback) get no new depth work until the lowest reaches 2; they still get defect fixes and whatever the foundation requires.

## Re-scores

(Appended as rounds land, each with its evidence.)
