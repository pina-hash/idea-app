# docs/standards/ -- register

One row per registered standards file. `Version` and `Date` come only from a
file's own header once it has been mirrored here; a file not yet present in
this directory shows `not yet mirrored` in both cells rather than a guess.

| Filename | Version | Date | Owns |
| --- | --- | --- | --- |
| `IDEA_instructions.md` | 4.29 | 2026-09-21 | Standing directive: doc-maintenance triggers, the freshness protocol for this directory (the fetch is a clone, not a raw URL), materials production pointer, Claude Code prompting and routing where the tier is chosen per prompt and is never a default -- Sonnet 5 for a docs-only bundle, Opus 5 at `high` for anything touching `src/`, any migration and any verification whose result will be relied on, Fable 5.1 only for what a session must decide on its own, and GPT-6 Astra as a Codex cloud task for volume work with no visual question in it, the audit phase, the claims rule, `tools/idea-status.py` first, decisions owed, merge cadence and the Deploy button, derived numbers are generated, cloud-container environment facts, git/branch/migration rules, per-repo migration apply paths, the ledger status lifecycle and the rule that prompts in flight come from the ledger rather than the branch list, the canned lane ending that now lets a lane apply its own migration through `tools/apply-migration.mjs` -- gated on the bundle's own ledger entry permitting one, and leaving a committed record under `docs/migrations-applied/` -- and merge `integration` into `main` against a six-item reported checklist, the migration-number rule that a number is allocated in the prompt and claimed in the session's first commit rather than read off the highest number on `origin/main`, and the model-configuration corrections of 4.25: `ultracode` is a Claude Code setting sending `xhigh` plus workflow orchestration rather than a rung above `max`, `max` carries a documented overthinking warning so the highest-scrutiny override sets `xhigh`, GitHub write access is per repository, and repository scope is fixed at session creation and cannot be set by prompt text, and the 4.27 rules: a prompt that vouches for a migration range names the floor, the ceiling, the permitted numbers and the halt and is re-read immediately before the merge, a manual instruction aimed at a web UI gives the URL rather than a route through the menus and asks for one URL rather than guessing an identifier, a push is a build and deploy cost is counted in pushes rather than landings, a decision entry's `Status` line is reconfirmed against the entry body rather than believed, and five communication rules Mr. Pina stated directly (an answer is as short as the question allows, every response names what to run now in one line, never guess a checkable fact, assume he knows nothing about current state, and do not hand him unrequested housekeeping), a long router chat maintaining a live state document on disk unprompted with a rule discovered mid-chat taking effect when it is discovered rather than at closeout -- plus the correction that code has the same delivered/landed/deployed three states a migration has, whose quiet third `IDEA_VERIFICATION_ADDENDA.md` rule 41 owns, and the 4.28 rule that replaces the old default outright: a tier carried forward from the previous prompt is not a choice, effort serialization is pointed at rather than restated, and the Codex constraints are cross-referenced to "Two agents, one repository", and the 4.29 rule that item 4 of the canned lane ending is satisfied by the probe exiting 0 or, where it cannot run, by a record-backed substitute over `docs/migrations-applied/` per decision 34 |
| `IDEA_MATERIALS_PROCESS.md` | 3.1 | 2026-08-28 | The process for authoring and publishing IDEA materials: pipeline, placement, tool roles, phases, the instrument-vs-material routing rule, the sources-citation standard, delivered filenames, notebook attachment, printed work surfaces, no weekday in student-facing copy, verifying a rendered artifact by what the file contains, regenerating rather than patching generated geometry, where mechanical drawings route, and "One File, One Fork" |
| `IDEA_MATERIAL_SPEC_v2.md` | 2.4 | 2026-08-27 | The canonical material authoring format: both kinds, the block types, the enforcement matrix, the instructions word ceiling, sentence floors set against the count of required items, and block ids unique across the whole spec |
| `IDEA_RUBRIC_STANDARDS.md` | 1.3 | 2026-08-25 | Leveled criteria, `short` forms, descriptor writing, grading behavior |
| `IDEA_HTML_ASSIGNMENT_TEMPLATE.md` | 1.0 | 2026-09-10 | How an author starts a new HTML assignment from `src/lib/legacy/assignments/_TEMPLATE.html`: what `data-field` and `data-min` do, the four things a copied assignment gets wrong (the storage key, the export filename prefix, the due date, and the rule that an export is a blank template plus its data), how to add a module, how the manifest maps fields to block ids and why a block id is permanent once students have answered, and how to put the file live |
| `IDEA_PRINT_STANDARDS.md` | 1.0 | 2026-08-10 | Rendering rules for print-first materials: page rules, typography, color in print, document structure |
| `IDEA_CLAUDE_DESIGN_STANDARDS.md` | 2.1 | 2026-08-27 | The IDEA pathway's sole hand-maintained design standard: visual identity (absorbed from the retired `IDEA_Design_System.md`) plus the scoping and prompting of every Claude Design artifact |
| `IDEA_INTERFACE_STANDARDS.md` | 2.12 | 2026-09-05 | Layout, viewport behaviour, role parity, legibility, interaction structure for shipped app surfaces |
| `IDEA_VERIFICATION_ADDENDA.md` | 2.7 | 2026-09-13 | The owning verification standard (no longer staging, despite the filename it deliberately keeps): how any build claim is proven, harnesses, mutation proof, positive controls, mock fidelity, the reporting channel itself -- a probe pasted into the Supabase SQL editor returns rows because that editor shows no `raise notice` and renders only the last statement's result set, a probe that examined nothing says so in its own output because an empty population and a clean result are different answers, and landed is not deployed, so a claim that a feature is live is a claim about the version string the server returns rather than about `main` -- and from 2.7 two rules about the instrument rather than the code under test: a mutant killed by a race (a mutation script run against a bare `npx vitest run` instead of `npm test`'s serial form) proves nothing, and a fixture resting on a fact about the live tree, such as which migration currently has no derivable probe, has an unwritten expiry date and must construct the condition rather than name something that satisfies it today |
| `IDEA_HUMOR_STANDARDS.md` | 2.2 | 2026-08-26 | Governs deliberate humor in IDEA and Team 5669 presentation artifacts: the three admission gates, sourcing, density and placement |
| `IDEA_HUMOR_LEDGER.md` | 1.4 | 2026-08-25 | The anti-repetition record and cooldown mechanism for every humor insertion. 1.3 is void and was never a real state; discard any copy carrying that number |
| `IDEA_REFERENCE_LIBRARY.md` | 4.3 | 2026-08-31 | Owning document for the three Google Drive libraries: what A and B hold, retrieval rules, consult-before-authoring, and Library C, the artifact store this assistant writes to at every delivery |
| `IDEA_Chat_Handoff_Standard.md` | 1.3 | 2026-09-02 | Governs every prompt written for a new claude.ai chat: when a handoff happens, the mandatory routing header, chat model routing (Opus 5 unless the queue is itself undecided), kickoff content rules including decisions owed at the top, and closing a chat, where the closing chat writes the closeout kickoff prompt itself |
| `IDEA_REPO_WORKFLOW_STANDARD.md` | 1.2 | 2026-09-10 | One development workflow across `idea-app`, `fll-app` and `frc-app`: `idea-app` as the reference, what every repo carries (`CLAUDE.md` sections, the three workflows, `docs/history`, ledger, decisions, status tool, harnesses, tests, numbered migrations), what differs on purpose, and the dated conformance table |
| `IDEA_CLASSROOM_REBUILD_PLAN.md` | 1.23 | 2026-09-23 | Records the locked decisions and phase history of the IDEA Classroom / notebook rebuild |
| `IDEA_MAPS_SPEC.md` | 1.2 | 2026-09-22 | The locked scoping decisions and build contract for IDEA Maps, the public spatial index of the IDEA building: domain model, draft-and-publish, search requirement, viewer, editor, phasing, verification requirements, and what was deliberately left undecided -- and from 1.2 the geometry reference face, which is decision 36's answer that a typed outline is the INTERIOR face of the space it describes, with wall thickness as a separate per-node value carrying a building-level default, uniform per node rather than per edge, drawn as a band lying outward, and snapping given a face chosen by the relationship (a parent is met by its inner face, a sibling by its outer one) |
| `IDEA_HTML_ASSIGNMENT_SPEC.md` | 1.1 | 2026-09-11 | The ported-HTML-assignment subsystem as built: what an HTML assignment is and how it differs from a `schemaVersion: 1` spec, the manifest schema and its validator, the security boundary (scripts and popups without `allow-same-origin`, the second origin, the CSP and its `sandbox` directive, and what the 0153 popup widening costs), the bridge protocol, what the sandbox costs, where each artifact is stored, the write gate that keeps the feature inert, validation at import, and how an existing document is ported. Carries the three corrections to the chat-delivered v1.0 that never reached the mirror -- `event.origin` is `"null"`, the CSP refuses all three IDEA typefaces, the CSP needed a `sandbox` directive -- and records the unrepresentable 1-point leveled criterion without resolving it |
| `IDEA_HTML_ASSIGNMENT_AUTHORING.md` | 1.1 | 2026-09-25 | What an author writing an HTML assignment does, in order: one self-contained file with any guide folded in, the manifest first with permanent block ids, the rubric and its hand edits, what the sandbox costs, opening things outside the frame (the Gmail compose URL over `mailto:`, never `noopener`), fluid width at both ends, height and the 2px scrollbar, IntersectionObserver probes clipped to the document, a follow-the-reader element, no layout changes inside a ResizeObserver callback, handing in a non-image file (extension check, acknowledgements matched in order, the in-document Download button), read-only and restore, both validators and the Chromium run, and delivery including the SQL query that replaces the grading export for HTML responses |
| `IDEA_context.md` | 2.0 | 2026-08-20 | Program-level facts that rarely change: school, pathway, courses, platforms, A-G status |
| `FRC_Design_System.md` | 1.6 | 2026-08-23 | FRC Team 5669's visual identity system: brand, marks, grounds, color roles, typography, motion, component manifest |
| `FRC_CLAUDE_DESIGN_STANDARDS.md` | 2.4 | 2026-08-25 | FRC counterpart to the IDEA Claude Design standard: scoping and prompting rules for every FRC artifact authored in Claude Design |

Every row above now carries a version and date read from line 2 of the file
itself, with no fallback and no footnote. The two exceptions this table used
to carry are closed: both files were delivered with a version header added.

## This table is maintained by a tool, not by memory

`tools/standards-sweep.py` in this repository reconciles this table against the
directory it describes and against the project knowledge copies, in every
direction at once, and reports each disagreement by name. Run it at the start of
any closeout and before editing any standards file in a batch. A row here is only
ever read from a **mirrored** file, so a row can be perfectly correct about the
mirror while the real current version of that file sits unmirrored in project
knowledge. That is not a defect in the register and the register cannot detect it;
the sweep can, and on 2026-08-29 it found three files in exactly that state.

## FRC files also live in a second repository

`FRC_Design_System.md` and `FRC_CLAUDE_DESIGN_STANDARDS.md` also live in
**`FRC-Team-5669-Techmen/frc-app`** at `src/lib/design-system/docs/`, for
Claude Code sessions working in that repo. **This directory
(`idea-app/docs/standards/`) is the freshness authority for both files
regardless of which repository a session is reading them from.**
