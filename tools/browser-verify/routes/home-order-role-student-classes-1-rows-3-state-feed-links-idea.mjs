/**
 * The home feed's two links under IDEA: the brass they always were at rest and
 * green on hover, so the dark theme renders exactly as before. The factory in
 * ./_home-feed-links.mjs is the spec; this file names the theme.
 */
import { homeFeedLinksSpec } from './_home-feed-links.mjs';

export default homeFeedLinksSpec('idea');
