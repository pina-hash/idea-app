/* See `roomSpec` in ./_theme-shared.mjs: the rain reaching into the notebook
   room (ledger 0117, report 25). */
import { roomSpec } from './_theme-shared.mjs';

export default roomSpec('notebook', 'nb-root');
