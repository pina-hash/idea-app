/**
 * The classroom masthead under the idea theme (ledger 0297, package F1b). The
 * factory in ./_masthead-theme.mjs is the spec; this file names the state.
 */
import { mastheadThemeSpec } from './_masthead-theme.mjs';

export default mastheadThemeSpec('idea');
