/** See `_classroom-profile-menu.mjs`: the IDEA theme's half of one list of claims. */
import { classroomProfileMenuSpec } from './_classroom-profile-menu.mjs';

export default classroomProfileMenuSpec('idea');
