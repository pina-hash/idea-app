/**
 * The classroom's hover ink under IDEA: the brass it always was, so the dark
 * theme renders exactly as before. The factory in ./_classroom-hover-ink.mjs is
 * the spec; this file names the theme.
 */
import { classroomHoverSpec } from './_classroom-hover-ink.mjs';

export default classroomHoverSpec('idea');
